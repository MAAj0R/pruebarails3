<%= form_for(genrecreate) do |f| %>
  <% if genrecreate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(genrecreate.errors.count, "error") %> prohibited this genrecreate from being saved:</h2>

      <ul>
      <% genrecreate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
